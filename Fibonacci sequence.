def fibonacci(n):
    if n <= 1:   # base cases
        return n
    else:
        return fibonacci(n-1) + fibonacci(n-2)  # recursive call

# Example
for i in range(10):
    print(fibonacci(i), end=" ")
